@if (Session["ModerRole"] != null)
{
    @Html.ActionLink("Список новостей", "ListOfRecords", "Manage")
}
 |
@if (Session["ModerRole"] != null)
{
    @Html.ActionLink("Редактировать новости", "EditRecord", "Manage")

}
 |